export interface Product {
  id: string;
  title: string;
  slug: string;
  categoryId: string;
  profileId: string;
  active: boolean;
  createdAt: string;
  description: string;
  updatedAt: string;
  storeId: string;
}
